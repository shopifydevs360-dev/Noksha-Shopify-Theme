{% liquid
  assign first_class = 'hero-info ' | append: 'text-' | append: ss.text_align
%}

<div class="{{ first_class }}">
    <h1 class="hero-title {{ ss.title_font_size }}">{{ ss.hero_title }}</h1>
    <div class="hero-description">{{ ss.hero_description }}</div>
</div>